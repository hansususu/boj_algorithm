#include <stdio.h>

int main() {
    int t, n, l;
    char s[20];
    scanf("%d", &t);
    for (int i = 0; i < t; i++) {
        scanf("%d", &n);
        int max = 0;
        char maxs[20];
        for (int j = 0; j < n; j++){
            scanf("%s %d", s, l);
            if (max < l) {
                max = l;
                for (int k = 0; k < )
            }
        }

    }
}